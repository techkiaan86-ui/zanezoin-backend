const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { const r = await prisma.role.findFirst({ where: { name: 'BUSINESS_CLIENT' }}); if (!r) return console.log('Role not found'); const perms = await prisma.roleMenu.findMany({ where: { roleId: r.id }, include: { menu: true }}); console.log(perms); } check().finally(() => prisma.$disconnect());
